Admin Reports → Deal Value shows lead counts, summed INR deal values and missing-value counts for every stage. Uses a protected backend aggregate over all leads, not the 500-row lead list. Deploy backend then frontend. No new migration or cron changes. All previous tasks, deletion, notifications and Brief features remain included. Production build and mocked API UI checks passed; live database execution was not tested here.
